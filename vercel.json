{
  "framework": "nextjs",
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @encerradigital/frontend-cliente build",
  "outputDirectory": "frontend-cliente/.next",
  "installCommand": "pnpm install --frozen-lockfile"
}
