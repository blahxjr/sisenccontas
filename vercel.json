{
  "framework": "nextjs",
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter ./frontend-cliente build",
  "outputDirectory": "frontend-cliente/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "devCommand": "pnpm --filter ./frontend-cliente dev"
}
